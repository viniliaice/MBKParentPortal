import { MarksScreen } from '@/components/MarksScreen';

/** The Marks tab: the same screen as the pushed /results route, without the back button. */
export default function MarksTabScreen() {
  return <MarksScreen />;
}
